let numbers = [1, 2, 3, 4, 5]; // Array of numbers
let fruits = ['apple', 'banana', 'orange', 'kiwi']; // Array of strings (fruits)

let combinearray = numbers.concat(fruits);
console.log(combinearray);